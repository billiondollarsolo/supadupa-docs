# Screenshot Assets

Committed screenshots are organized by feature area:

- `actual/light/`
- `actual/dark/`

Use deterministic demo data and redact secrets before committing screenshots.

The `actual/` folders contain the real screenshot set copied from the main `supadupa` repository. The gallery page at `docs/admin-ui/screenshots.mdx` embeds every image from both color modes.
